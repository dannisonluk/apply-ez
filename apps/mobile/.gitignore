# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/

# expo-env.d.ts is committed on purpose: it is normally regenerated by the dev
# server, but committing it means `npm run typecheck` works on a fresh clone.

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*.local

# typescript
*.tsbuildinfo

# generated native folders
/ios
/android

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

# NOTE: `expo-env.d.ts` is listed here by expo-cli, but it is COMMITTED ON PURPOSE
# and this rule does not untrack it — git ignores .gitignore for files already in
# the index. It has to stay tracked so `npm run typecheck` works on a fresh clone
# without running the dev server first. This note lives here rather than inside that
# file because `expo start` rewrites expo-env.d.ts every run, so a comment there is
# deleted on the next start and shows up as churn.
expo-env.d.ts
# @end expo-cli